/**
 * Centralized default parameter configuration for all SentinelBench tasks
 * Provides consistent defaults across all tasks based on task type and difficulty
 */

/**
 * 
 * currently not implemented, I am debating between having separate files for each
 * one of the main fields, taskNames, taskDefaults, taskPasswords, or making the pages 
 * into folders for distinct tasks, which would make it much simpler and more modular 
 * to add more tasks in the future
 * 
 */